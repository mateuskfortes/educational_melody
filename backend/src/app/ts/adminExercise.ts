// Legacy express handlers removed. These were the old server routes and are
// intentionally left as an empty module to avoid being picked up by the
// Next.js TypeScript build. Functionality has been migrated to app/* route
// handlers (see app/api and app/admin/... route files).

export {}